/**
 * Claude Code MCP Client Capabilities
 */

import { MCPClientCapabilities } from '../types';

const claudeDesktopCapabilities: MCPClientCapabilities = {
  prompts: {
  },
  resources: {
  },
  tools: {
  }
};

export default claudeDesktopCapabilities;
